<!DOCTYPE html>
<html>
  <head>
    <title>Bankr</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body id="backimg">

    <nav class="navbar is-dark">
      <div class="navbar-menu">
        <div class="logo"> <img src="https://image.flaticon.com/icons/svg/3010/3010849.svg"> </div>
        <div class="navlink"> <button class="navbutton"> <%= link_to "View All Banks", banks_path %> </button> </div>

        <% if session[:user_id]%>
          <div class="navlink"> <button class="navbutton"> <%= link_to "User homepage", user_path(session[:user_id])%> </button> </div>
          <div class="navlink"> <button class="navbutton"> <%= link_to "Log out", logout_path %> </button> </div>
        <% else %>
          <div class="navlink"> <button class="navbutton"> <%= link_to "Log In", login_path %> </button> </div>
        <% end %>
      </div>
    </nav>

    <section id="bg">
      <div class="container is-large" id="main_container">
        <%= yield %>
      </div>
    </section>

    <% if false %>
      <footer>
        <div class="footer"> This was a TOUGH Project !! </div>
      </footer>
    <% end %>

    <% if false %>
      <% #THIS IS COMMENTED OUT CODE TO SHOW DEBUG PARAMS IN DEVELOPER ENVIRONMENT %>
      <section id="railshelper">
        <div class="debughelper" id="dh">
          <%= debug(params) if Rails.env.development? %>
        </div>
      </section>

    <% end %>

  </body>

</html>

