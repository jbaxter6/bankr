<h1>Transfer history</h1>

<ul>
<% @transfers.each do |transfer| %>
  <li><%= "#{transfer.sender.user.username} sent $#{transfer.amount} to #{transfer.receiver.user.username} at their #{transfer.receiver.bank.name} account." %></li>
<% end %>
</ul>
